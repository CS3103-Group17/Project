package util;

public final class Constants {
    private Constants() {}
    
    public enum Action {
        SEARCH
    } 
    
    public enum Category {
        ALL, FLIGHTS, HOTELS, SOCIALS, TRAVELS
    }
    
    public enum Hotels {
        EXPEDIA
    }
    
    public enum Flights {
        SKYSCANNER
    }
    
    public enum Socials {
        TWITTER, INSTAGRAM
    }
    
    public enum Travels {
        WIKITRAVEL
    }
}
